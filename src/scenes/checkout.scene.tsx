import React from "react";
import { CenterLayout } from "@/layout/center.layout";
import { CheckoutContainer } from "@/pods/gallery";

export const CheckoutScene: React.FC = () => {
  
  return (
    <CenterLayout>
        <CheckoutContainer />
    </CenterLayout>
  );
};