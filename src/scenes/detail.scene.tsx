import React from "react";
import { CenterLayout } from "@/layout";
import { DetailContainer } from "@/pods/list/detail.container";

export const DetailScene: React.FC = () => {

  return (
    <CenterLayout>
        <DetailContainer />
    </CenterLayout>
  
  );
};
