import React from "react";
import { CenterLayout } from "@/layout";
import { ImageContainer } from "@/pods";

export const GalleryScene: React.FC = () => {

  return (
    <CenterLayout>
        <ImageContainer />
    </CenterLayout>
  
  );
};